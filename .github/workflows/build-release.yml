name: Build Plugin ZIP

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Prepare plugin
        run: |
          mkdir -p build/media-optimizer
          rsync -av --exclude='.git' --exclude='.github' --exclude='build' ./ build/media-optimizer/

      - name: Create ZIP
        run: |
          cd build
          zip -r media-optimizer.zip media-optimizer

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: media-optimizer
          path: build/media-optimizer.zip

      - name: Publish GitHub Release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: build/media-optimizer.zip
          generate_release_notes: true
